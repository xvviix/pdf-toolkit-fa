# ─────────────────────────────────────────────────────────────
#  PDF Toolkit v12 — وابستگی‌های اصلی (سبک و سریع)
#  نصب:  pip install -r requirements.txt
#
#  نکته: پایتون 3.10 تا 3.13 توصیه می‌شود.
#        پایتون 3.14+ هنوز توسط موتور OCR پشتیبانی نمی‌شود.
# ─────────────────────────────────────────────────────────────

# هسته PDF و تصاویر
pypdf>=4.0
PyMuPDF>=1.24
Pillow>=10.0

# ─────────────────────────────────────────────────────────────
#  اختیاری: OCR (تشخیص خودکار نام فارسی)
#  فقط در صورت نیاز جداگانه نصب کنید:
#     pip install -r requirements-ocr.txt
#  برنامه بدون آن هم کامل کار می‌کند (حالت نام‌گذاری دستی).
# ─────────────────────────────────────────────────────────────
